const NotFound = () => {
  return (
    <div
      style={{
        fontSize: "1.5rem",
        marginTop: "3rem",
        display: "flex",
        justifyContent: "center",
        color: "#FFFFFF",
      }}
    >
      Page not found
    </div>
  );
};

export default NotFound;
